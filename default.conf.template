server {
    listen 80;

    location / {
        root /usr/share/nginx/html;
        index index.html;
    }

    location /env {
        add_header Content-Type text/plain;
        return 200 "${JESPER_SIN_GMAPS_API_KEY}";
    }
}